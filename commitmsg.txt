Phase 2: Contact-Form + Heizlast-Projects auf D1 + Resend umgestellt

- functions/api/contact.js: schreibt Anfragen in D1 (contact_submission),
  schickt Benachrichtigung via Resend an daniel@thermowerk.ch
  (Absender noreply@, reply-to = Kundenmail). Web3Forms + Sanity raus.

- functions/api/heizlast-projects.js: alle Sanity-Fetches durch
  env.DB.prepare() ersetzt. GET-Liste / GET-Single / POST (UPSERT via
  ON CONFLICT) / DELETE. Response-Shape bleibt client-kompatibel
  (_id, projectName, projects[]).

- src/pages/index.astro: Client-JS auf einen einzigen Fetch an
  /api/contact konsolidiert. WEB3FORMS_KEY + zweite fetch-Kette raus.
