Heizlast S7 - Topbar: Logo (logo alt.png) ohne Anschrift, Container-Bug fix

* Logo (public/img/logo alt.png) wird jetzt nach public/heizlast/img/Logo.png
  gespiegelt und via relativem Pfad ./img/Logo.png eingebunden — kein
  externer Asset-Request, kein Cache-Problem.
* Anschrift entfernt — Logo allein reicht (User-Wunsch). Sub-Title
  "Heizlast-Auslegung" rechts daneben bleibt.
* Topbar-Layout-Bug: Brand-Row und Action-Row hatten Padding direkt am
  flex-Container, was bei Container-Klasse (max-width 1100px) inkonsistent
  zur App-Main-Container-Breite war (links uebersteh, rechts buendig).
  Refactor: full-width-Outer-Row + .topbar__inner mit max-width 1100px,
  margin: 0 auto und padding — gleicher Centering-Mechanismus wie der
  rest der Seite, jetzt buendig auf beiden Seiten.
