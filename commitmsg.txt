Phase 10 Block 3: Cutover auf v2-Komponenten-Suite

- src/pages/heizlast.astro: komplett neu geschrieben (~700 Zeilen).
  Imports jetzt aus src/components/heizlast-v2/ und
  ../layouts/HeizlastLayout.astro. Inline topbar mit brand +
  project-tag + login-trigger. Mount-Order: PrintCover -> topbar ->
  Section 1-4 -> Section5aDiagramm -> Section5Speicher ->
  Section6Projekt -> 3 Modale. Glue-JS an v2-Selektoren angepasst:
  Zimmer als .z-table tbody mit 7 Spalten (col-num|col-area|col-chk|
  col-act), Sanierung als .measure-row, Perioden als .period
  .period-{verbrauch|messung|bstd}. Methodenwahl ueber nativen
  <details name="heizlast-method"> Exclusive-Accordion (toggle-Event
  -> methodsEnabled Record). Gate-/Addon-Sync via
  requestAnimationFrame. WW-Panes per data-active="true" Attribut.
  Notes-Dot toggelt .has-content. Focus-Trap auf .modal,
  Login-Form #f-login-pw, Export-Form #f-export-name +
  name="part-*" + name="export-fmt". beforeunload erhalten.
- src/layouts/HeizlastLayoutV2.astro -> HeizlastLayout.astro
  (alte v1-Datei geloescht, dann via move /y umbenannt).
  Print-CSS Zeile 1278: #sektion-diagramm -> #sektion-5a.
- src/components/heizlast/ komplett geloescht (v1-Suite):
  Section1Gebaeude..Section7Projekt, ExecutiveSummary, InfoBox,
  KpiCard, Toggle, SectionNotes, LeistungsDiagramm, LoginModal,
  ProjectsModal, ExportModal, PrintCover, OverrideField,
  SectionWrapper.

Tests weiterhin gruen: 49 Rechenkern + 43 State-Integration.
FWS-Aufgabe 2: Qhl = 12.5453 kW (Referenz 12.55, Tol 0.02).

Damit ist Phase 10 (kompletter v2-Port, drei Bloecke) abgeschlossen.
