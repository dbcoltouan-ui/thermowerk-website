fix(heizlast): compute chain live — methodsEnabled.verbrauch default + OverrideField zeigt Defaults

- createDefaultState(): methodsEnabled.verbrauch=true (war false).
  Grund: Section2Heizlast rendert M1 mit openDefault=true, aber der
  Browser feuert kein toggle-Event auf SSR-Initialstand. Folge: Compute
  fiel immer auf qhlBauperiode zurueck, User-Eingaben im Verbrauch-Block
  (Ba, Perioden) erreichten den aktiven Methoden-Branch nicht.

- bindings.ts syncDomFromState: Wenn bindPath null/leer und der Wrapper
  ein [data-hz-override-field] ist und der User nichts ueberschrieben hat,
  zeigen wir resolveDefault(state, overridePath). Damit erscheint
  Tvoll live aus Lage + Nutzungsprofil, deltaT aus PHYSIK, WW-Verluste
  aus FWS-Defaults etc. — und Lage-Wechsel aktualisiert das Feld sofort.

- resolveDefault-Import in bindings.ts.

Tests: 49 Rechenkern + 43 State-Integration weiterhin gruen. FWS-Aufgabe 2
Qhl = 12.5453 kW (Referenz 12.55, Tol 0.02).
