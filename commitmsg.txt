Phase 10 / Paket D4 - Container schmaler + Grid-Fluchten

Zwei gezielte Aenderungen in src/layouts/HeizlastLayout.astro:

1. --hz-container-max von min(92vw, 1120px) auf min(86vw, 980px).
   Klar sichtbarer Aussenrand auf 1440-1920 px Bildschirmen.

2. .hz-grid-2/3 von repeat(N, max-content) auf repeat(N, 1fr).
   Inputs innerhalb von Grids fuellen die Zelle (max-width !important).
   Dadurch liegen Label- und Input-Kanten auf gemeinsamen Fluchten.
   data-size bleibt fuer Felder ausserhalb von Grids wirksam.

Tests bleiben gruen (49 + 33).
