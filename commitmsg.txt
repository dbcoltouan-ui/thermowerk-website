heizlast: Projekte-Modal konsolidieren + Remote-Delete-Drift

- Eine einzige Projekt-Liste im Modal statt getrennter lokal/Cloud-Listen.
- Matching ueber meta.id <-> _id: jedes Projekt nur einmal sichtbar.
- Badge "Cloud" (synchronisiert) oder "Nur lokal" (nur lokal vorhanden).
- Neue Hilfsfunktion markLocalAsCloudMirror: markiert lokale Snapshots
  beim Speichern (Cloud OK) mit meta.cloudMirror=true.
- cloudList() macht nach dem Fetch Drift-Cleanup: lokale Mirror-Eintraege,
  die nicht mehr in der Cloud sind (auf anderem Geraet geloescht),
  werden lokal entfernt. Rein lokale Projekte bleiben.
- HTML: zweiter Listen-Block (cloud-only--block) entfernt.
- refreshProjectsList baut via buildMergedProjects die vereinte Liste.
- Click-Routing unveraendert: origin="cloud" -> cloudLoad/cloudDelete,
  origin="local" -> loadProject/deleteProject.

Deployed: public/heizlast/index.html gespiegelt.
