handoff: Phase 9 - Block M1 ergaenzt (Desktop-Breite + Fluid-Typografie)

- Neuer Block M1 in HANDOFF-PHASE-9.md: Container-Max auf
  min(92vw, 960px) reduzieren, damit links/rechts auf grossen
  Monitoren mehr Rand entsteht und der Content ruhiger wirkt
- Fluid-Typografie via clamp() fuer Body, Section-Titel, Kicker,
  Subline, KPI-Zahlen und Form-Inputs - skaliert zwischen Mobile
  und Desktop fliessend, prozentualer Seiten-Eindruck bleibt gleich
- Abstands-Tokens (--hz-gap-section/row/card) auf clamp() umstellen
- Grid-Minmax auf clamp(160px, 18vw, 220px) damit Felder symmetrisch
  und ruhig bleiben, keine zu kleinen Micro-Spalten
- Akzeptanzliste um Desktop-Breite-Kriterium ergaenzt
