heizlast: Effektives V_W,u in Sektion 3 nur noch in Personen-Modus + live

Im Direkt-Modus war das readonly-Feld 'Effektives V_W,u' identisch
zum Eingabefeld 'Nutzvolumen' — redundant. Wird dort jetzt
ausgeblendet.

Im Personen-Modus bleibt es stehen (dort ist es tatsaechlich das
Produkt Personen x l/P.d) und aktualisiert sich jetzt live, statt
erst nach komplettem Rebuild. Gleicher targeted-DOM-Update wie beim
Sektion-2-Hint (wwInclEffV), neue id: wwEffVInput.
