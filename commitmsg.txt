Phase 9 / Block D: Sperrzeit-Toggle + Qas-Klarstellung

- Section 4: Sperrzeit Ja/Nein-Toggle (zuschlaege.sperrzeitActive),
  toff-Feld sichtbar nur bei aktivem Gate. Qas umbenannt zu
  "Qas - Verbundene Systeme" mit Hints (Pool 1-3 kW, Lueftung 0.3-0.8 kW).
- compute.ts: computeQoff gated auf sperrzeitActive. qasActive-Gate
  entfernt, Qas wird automatisch addiert wenn > 0.
- state.ts: neues Feld sperrzeitActive: boolean. qasActive bleibt als
  Legacy-Field (wird ignoriert).
- storage.ts: sanfte Migration — alte States ohne sperrzeitActive
  bekommen sperrzeitActive = (toff > 0).
- Tests: test-heizlast-state.ts um Block-D-Sektion erweitert (Qoff-Gate,
  Qas-Auto-Addition). Phase 3 Integrations-Tests 19 gruen, Rechenkern
  49 weiterhin gruen. FWS-Aufgabe 2: Qhl = 12.55 kW stabil.
- .gitignore: _backup-*.zip ausgeschlossen.
