Heizlast S7 - Export-Bug: bedingungen ist String statt Array

* In foerderungen.json haben einige Programme p.bedingungen als String
  statt Array - der Code hat blind (p.bedingungen || []).join('; ')
  aufgerufen, was bei String-Werten "join is not a function" wirft.
* Fix: Array.isArray-Check, fallback to direct String-Output.
