Phase 9 / Block M: Mobile-Polish (Worttrennung + Logo)

- .hz-brand__text Span komplett entfernt (HTML + CSS), nicht nur
  display:none -> saubere Logo-Kante
- Mobile-Logo auf 32 px (Container nicht mehr gestaucht)
- Header-Height Mobile 60 px
- Konservative Worttrennung im .hz-scope: hyphens: manual +
  overflow-wrap: break-word + word-break: normal -> keine mid-word-
  Breaks mehr
- text-wrap: balance auf H1-H3, Section-Title, Section-Subline, Kicker
- Mobile-Regel ersetzt das bisherige overflow-wrap: anywhere
- Desktop-Logo buendig mit .hz-container-Kante (bereits durch Block M1
  via --hz-container-max sichergestellt)
- Tests weiterhin 49 + 33 gruen