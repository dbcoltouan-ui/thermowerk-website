---
// Basis-Layout für alle Seiten
interface Props {
  title?: string;
  description?: string;
}
const {
  title = "Thermowerk – Wärmepumpen & Klimaanlagen | Neftenbach, Zürich",
  description = "WPSM-zertifizierter Fachbetrieb für Luft-Wasser-Wärmepumpen. Persönliche Beratung, fachgerechte Montage und Förderberatung in ZH, SH, TG, AG."
} = Astro.props;
---
<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
  <title>{title}</title>
  <meta name="description" content={description}>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=Outfit:wght@400;500;600;700;800&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style is:global>
    /* ==========================================
       THERMOWERK – STYLESHEET
       ========================================== */

    /* 1. VARIABLEN */
    :root {
      --navy: #1B2A4A;
      --navy-light: #243658;
      --navy-dark: #111D33;
      --red: #D93025;
      --red-hover: #B71C1C;
      --white: #FFFFFF;
      --off-white: #F7F8FA;
      --light-gray: #EEF0F4;
      --mid-gray: #6B7280;
      --dark: #1A1A1A;
      --border: #E2E5EB;
      --blue-tint: #E8EDF5;
      --font-heading: 'Outfit', sans-serif;
      --font-body: 'DM Sans', sans-serif;
      --header-height: 88px;
      --hero-left-offset: 0px;
    }

    /* 2. RESET & BASIS */
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; overflow-x: hidden; }
    body { font-family: var(--font-body); font-size: 17px; line-height: 1.7; color: var(--dark); background: var(--white); -webkit-font-smoothing: antialiased; overflow-x: hidden; }
    img { max-width: 100%; display: block; }
    a { color: inherit; text-decoration: none; }

    /* 3. LAYOUT */
    .container { max-width: 1120px; margin: 0 auto; padding: 0 clamp(16px, 3vw, 24px); }
    .section-padding { padding: clamp(60px, 10svh, 100px) 0; }
    .text-center { text-align: center; }

    /* 4. TYPOGRAFIE */
    h1, h2, h3, h4 { font-family: var(--font-heading); font-weight: 700; line-height: 1.2; color: var(--dark); }
    h1 { font-size: clamp(34px, 5vw, 52px); }
    h2 { font-size: clamp(28px, 3.8vw, 40px); margin-bottom: clamp(14px, 2.5svh, 20px); }
    h3 { font-size: clamp(19px, 2.2vw, 22px); margin-bottom: clamp(6px, 1svh, 8px); }
    h4 { font-size: clamp(17px, 1.9vw, 19px); font-weight: 600; }
    .section-subtitle { font-size: clamp(13px, 1.5vw, 15px); font-weight: 600; letter-spacing: 2px; text-transform: uppercase; color: var(--red); margin-bottom: clamp(8px, 1.5svh, 12px); font-family: var(--font-heading); }
    .section-subheading { font-family: var(--font-heading); font-size: clamp(20px, 2.5vw, 26px); font-weight: 500; color: var(--mid-gray); line-height: 1.4; margin-top: clamp(-10px, -1.5svh, -6px); margin-bottom: clamp(14px, 2.5svh, 20px); }

    /* 5. BUTTONS */
    .btn { display: inline-flex; align-items: center; gap: 8px; font-family: var(--font-heading); font-weight: 600; font-size: clamp(14px, 1.6vw, 16px); padding: clamp(12px, 1.8svh, 15px) clamp(24px, 3vw, 34px); border-radius: 3px; border: none; cursor: pointer; transition: all 0.3s ease; text-decoration: none; }
    .btn-primary { background: var(--red); color: var(--white); }
    .btn-primary:hover { background: var(--red-hover); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(217, 48, 37, 0.3); }
    .btn-outline { background: transparent; color: var(--white); border: 2px solid rgba(255, 255, 255, 0.4); }
    .btn-outline:hover { border-color: var(--white); background: rgba(255, 255, 255, 0.1); }
    .btn-dark { background: var(--navy); color: var(--white); }
    .btn-dark:hover { background: var(--navy-light); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(27, 42, 74, 0.3); }
    .btn-outline-dark { background: transparent; color: var(--navy); border: 2px solid var(--navy); }
    .btn-outline-dark:hover { background: var(--navy); color: var(--white); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(27, 42, 74, 0.2); }
    /* Einheitliche CTA-Button-Breiten – zentriert via margin auto */
    .btn-cta { display: flex; width: fit-content; min-width: clamp(200px, 18vw, 240px); justify-content: center; text-align: center; margin-left: auto; margin-right: auto; }
    .btn-cta-lg { display: flex; width: fit-content; min-width: clamp(260px, 24vw, 320px); justify-content: center; text-align: center; margin-left: auto; margin-right: auto; }

    /* 6. HEADER */
    .header { position: fixed; top: 0; left: 0; right: 0; z-index: 1000; background: #FFFFFF; border-bottom: none; transition: background 0.3s ease, box-shadow 0.3s ease; padding-top: env(safe-area-inset-top, 0px); }
    .header.scrolled { background: #FFFFFF; box-shadow: 0 1px 12px rgba(0, 0, 0, 0.06); }
    .header-inner { display: flex; align-items: center; justify-content: space-between; height: var(--header-height); max-width: 1120px; margin: 0 auto; padding: 0 24px; }
    .logo-img { height: clamp(54px, 6vw, 66px); width: auto; }
    .nav { display: flex; align-items: center; gap: clamp(20px, 3vw, 44px); }
    .nav > a, .nav-dropdown-trigger { font-family: 'Montserrat', var(--font-heading); font-size: 18px; font-weight: 500; letter-spacing: -0.1px; color: var(--dark); transition: color 0.2s; position: relative; cursor: pointer; display: inline-flex; align-items: center; text-decoration: none; }
    .nav > a:hover, .nav-dropdown-trigger:hover { color: var(--navy); }
    .nav > a::after, .nav-dropdown-trigger::after { content: ''; position: absolute; bottom: -4px; left: 0; width: 0; height: 2px; background: var(--red); transition: width 0.3s ease; }
    .nav > a:hover::after, .nav-dropdown-trigger:hover::after { width: 100%; }
    .nav .btn-primary { color: var(--white) !important; font-size: 14px; padding: 10px 24px; white-space: nowrap; }
    .nav .btn-primary::after { display: none; }
    .mobile-menu-link { display: none !important; }
    .mobile-menu-buttons { display: none !important; }
    .btn-stoerung { display: none !important; }
    .btn-stoerung::after { display: none !important; }
    .footer-stoerung { display: inline-flex; align-items: center; gap: 6px; color: var(--red) !important; font-weight: 600; border: 1px solid rgba(217, 48, 37, 0.3); padding: 6px 14px; border-radius: 3px; font-size: 13px; transition: all 0.3s ease; }
    .footer-stoerung:hover { background: var(--red); color: var(--white) !important; border-color: var(--red); }
    /* Dropdown */
    .nav-dropdown { position: relative; }
    .nav-dropdown-menu { position: absolute; top: 100%; left: -16px; padding-top: 12px; opacity: 0; visibility: hidden; transform: translateY(4px); transition: all 0.25s ease; z-index: 100; }
    .nav-dropdown:hover .nav-dropdown-menu { opacity: 1; visibility: visible; transform: translateY(0); }
    .nav-dropdown-menu a { display: block; padding: 10px 20px; font-family: 'Montserrat', var(--font-heading); font-size: 14px; font-weight: 500; color: var(--dark); white-space: nowrap; background: var(--white); border: 1px solid var(--border); border-top: none; transition: background 0.2s, color 0.2s; }
    .nav-dropdown-menu a:first-child { border-top: 1px solid var(--border); border-radius: 3px 3px 0 0; }
    .nav-dropdown-menu a:last-child { border-radius: 0 0 3px 3px; }
    .nav-dropdown-menu a::after { display: none; }
    .nav-dropdown-menu a:hover { background: var(--blue-tint); color: var(--navy); }
    .menu-toggle { display: none; flex-direction: column; gap: 6px; cursor: pointer; background: none; border: none; padding: 8px; z-index: 1002; }
    .menu-toggle span { display: block; width: 24px; height: 2px; background: var(--dark); border-radius: 2px; transition: transform 0.3s ease, opacity 0.3s ease; transform-origin: center; }
    .menu-toggle.active span:nth-child(1) { transform: translateY(8px) rotate(45deg); }
    .menu-toggle.active span:nth-child(2) { opacity: 0; }
    .menu-toggle.active span:nth-child(3) { transform: translateY(-8px) rotate(-45deg); }

    /* 7. HERO – milchiger Glaseffekt */
    .hero { position: relative; min-height: 100svh; display: flex; align-items: center; background: #FFFFFF; overflow: hidden; }
    /* NOTIZ: Einstellung "rechte 7/8" war: left: 12.5%, object-position: 60% center, Gradient: 0%→32% voll weiss, 50% 0.7, 70% 0.3, 100% 0.15 */
    .hero-bg { position: absolute; inset: 0; z-index: 1; }
    .hero-bg img { width: 100%; height: 100%; object-fit: cover; object-position: 85% center; }
    .hero-img-mobile { display: none; }
    .hero-img-desktop { display: block; }
    .hero-overlay { position: absolute; inset: 0; z-index: 2; background: linear-gradient(180deg, rgba(255,255,255,0.92) 0%, rgba(255,255,255,0.75) 40%, rgba(255,255,255,0.45) 70%, rgba(255,255,255,0.2) 100%); }
    .hero-inner { position: relative; z-index: 3; width: 100%; max-width: 1120px; margin: 0 auto; padding: 0 24px; padding-top: var(--header-height); min-height: 100svh; display: flex; align-items: flex-start; padding-top: calc(var(--header-height) + clamp(40px, 8svh, 100px)); }
    .hero-content { max-width: 660px; }
    .hero-box { background: rgba(255,255,255,0.55); backdrop-filter: blur(2px); -webkit-backdrop-filter: blur(2px); border-radius: 12px; padding: 8px 16px 16px 16px; display: inline-flex; flex-direction: column; }
    .hero-label { font-family: var(--font-heading); font-size: clamp(16px, 1.7vw, 20px); font-weight: 600; letter-spacing: 2px; text-transform: uppercase; color: var(--red); margin-bottom: clamp(12px, 2svh, 20px); }
    .hero h1 { color: var(--navy); margin-bottom: clamp(4px, 1svh, 10px); font-weight: 800; font-size: clamp(36px, 4.8vw, 60px); letter-spacing: -0.5px; line-height: 1.08; }
    .hero-subtitle { font-family: var(--font-heading); font-size: clamp(22px, 3vw, 36px); font-weight: 500; color: var(--navy-light); margin-bottom: clamp(16px, 3svh, 28px); line-height: 1.3; }
    .hero-text { font-size: clamp(16px, 1.8vw, 22px); color: var(--navy); margin-bottom: clamp(24px, 4svh, 36px); line-height: 1.7; font-weight: 500; width: 0; min-width: 100%; text-wrap: pretty; }
    .hero-buttons { display: flex; align-items: center; gap: clamp(12px, 1.5vw, 16px); flex-wrap: wrap; margin-bottom: clamp(20px, 3svh, 32px); width: 0; min-width: 100%; }
    .hero-badge { display: flex; align-items: center; gap: 10px; font-family: var(--font-heading); font-size: clamp(12px, 1.3vw, 14px); font-weight: 500; color: var(--navy); letter-spacing: 0.5px; width: 0; min-width: 100%; }
    .hero-badge svg { color: var(--red); flex-shrink: 0; }
    .hero-scroll { display: none; }
    .scroll-line { display: none; }

    /* 8. ÜBER UNS (neues Layout mit Bild rechts) */
    .about { background: var(--white); }
    .about-intro { display: grid; grid-template-columns: 1.3fr 0.7fr; gap: clamp(32px, 4vw, 48px); align-items: start; margin-bottom: clamp(32px, 5svh, 48px); }
    .about-intro-text p { font-size: clamp(14px, 1.6vw, 16px); color: var(--mid-gray); line-height: 1.7; }
    .about-claim { font-family: var(--font-heading); font-size: clamp(17px, 2vw, 20px); font-weight: 700; color: var(--dark); margin-top: clamp(16px, 3svh, 24px); }
    .about-intro-img { }
    .img-placeholder-box { background: var(--off-white); border: 2px dashed var(--border); border-radius: 2px; height: 380px; display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 12px; color: var(--mid-gray); font-size: 14px; font-family: var(--font-heading); }
    .about-usp-heading { margin-bottom: clamp(16px, 3svh, 24px); }
    .about-usp-heading h3 { font-size: clamp(20px, 2.5vw, 24px); color: var(--navy); }
    .about-usp-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: clamp(16px, 2.5vw, 24px); margin-bottom: clamp(32px, 5svh, 48px); }
    .about-usp-card { background: var(--off-white); border-radius: 2px; padding: clamp(20px, 3svh, 28px) clamp(18px, 2.5vw, 24px); border: 1px solid var(--border); transition: transform 0.3s, box-shadow 0.3s; }
    .about-usp-card:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(0, 0, 0, 0.06); }
    .about-usp-icon { width: clamp(40px, 5vw, 48px); height: clamp(40px, 5vw, 48px); background: var(--navy); border-radius: 2px; display: flex; align-items: center; justify-content: center; margin-bottom: clamp(12px, 2svh, 16px); }
    .about-usp-icon svg { width: clamp(20px, 2.5vw, 24px); height: clamp(20px, 2.5vw, 24px); stroke: var(--white); fill: none; stroke-width: 1.8; }
    .about-usp-card h4 { margin-bottom: clamp(6px, 1svh, 8px); font-size: clamp(15px, 1.7vw, 17px); color: var(--navy); }
    .about-usp-card p { font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); line-height: 1.65; }
    .about-closing { max-width: 800px; }
    .about-closing p { font-size: clamp(14px, 1.6vw, 16px); color: var(--mid-gray); line-height: 1.7; }

    /* 9. LEISTUNGEN (Service-Karten – kaeltech-Style mit Bild oben) */
    .services { background: var(--off-white); padding-bottom: clamp(37px, 6.2svh, 62px); }
    .services-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: clamp(20px, 2.5vw, 28px); margin-top: clamp(32px, 5svh, 48px); }
    .service-card { display: flex; flex-direction: column; background: var(--white); border-radius: 2px; overflow: hidden; border: 1px solid var(--border); transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s; text-decoration: none; color: inherit; }
    .service-card:hover { transform: translateY(-6px); box-shadow: 0 16px 40px rgba(0, 0, 0, 0.08); border-color: var(--navy); }
    .service-card-img { height: clamp(160px, 20svh, 200px); overflow: hidden; background: linear-gradient(135deg, var(--navy) 0%, var(--navy-light) 100%); }
    .service-card-img img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s ease; }
    .service-card:hover .service-card-img img { transform: scale(1.05); }
    .service-img-placeholder { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: rgba(255, 255, 255, 0.35); }
    .service-card-body { padding: clamp(20px, 3svh, 28px) clamp(18px, 2.5vw, 24px); display: flex; flex-direction: column; flex: 1; }
    .service-card h3 { color: var(--navy); font-size: clamp(18px, 2vw, 20px); margin-bottom: clamp(8px, 1.5svh, 12px); }
    .service-card p { font-size: clamp(14px, 1.5vw, 15px); color: var(--mid-gray); line-height: 1.65; flex: 1; }
    .service-link { display: inline-flex; align-items: center; justify-content: center; font-family: var(--font-heading); font-weight: 600; font-size: clamp(14px, 1.5vw, 15px); color: var(--navy); margin-top: clamp(14px, 2.5svh, 20px); padding: clamp(8px, 1.2svh, 10px) clamp(18px, 2.5vw, 24px); border: 2px solid var(--navy); border-radius: 3px; transition: all 0.3s ease; align-self: center; }
    .service-card:hover .service-link { background: var(--navy); color: var(--white); }

    /* 9a. HERSTELLERLOGOS */
    .logos-bar { background: var(--white); padding: clamp(6px, 0.9svh, 9px) 0; position: relative; }
    .logos-bar::after { content: ''; display: block; height: clamp(37px, 6.2svh, 62px); background: var(--off-white); position: absolute; bottom: 0; left: 0; right: 0; transform: translateY(100%); }
    .logos-track { display: flex; align-items: center; justify-content: center; gap: clamp(24px, 4vw, 48px); flex-wrap: nowrap; }
    .logos-track img { height: clamp(44px, 5.5vw, 64px); width: clamp(140px, 16vw, 220px); object-fit: contain; transition: transform 0.3s ease, filter 0.3s ease; }
    .logos-track img.logo-sm { width: clamp(79px, 9vw, 124px); }
    .logos-track img.logo-md { width: clamp(108px, 12.2vw, 168px); }
    .logos-track img:hover { transform: scale(1.1); filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.15)); }

    /* 10. ABLAUF */
    .steps { background: var(--off-white); }
    .steps-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: clamp(14px, 2vw, 20px); margin-top: clamp(32px, 5svh, 48px); }
    .step-card { background: var(--white); border-radius: 2px; padding: clamp(24px, 3.5svh, 32px) clamp(18px, 2.5vw, 24px); border: 1px solid var(--border); transition: transform 0.3s, box-shadow 0.3s; }
    .step-card:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(0, 0, 0, 0.06); }
    .step-number { font-family: var(--font-heading); font-size: clamp(36px, 4.5vw, 48px); font-weight: 800; color: var(--light-gray); line-height: 1; margin-bottom: clamp(12px, 2svh, 16px); }
    .step-card h3 { color: var(--navy); font-size: clamp(16px, 1.8vw, 18px); margin-bottom: clamp(8px, 1.5svh, 12px); }
    .step-card p { font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); line-height: 1.6; }
    .steps-cta { text-align: center; margin-top: clamp(32px, 5svh, 48px); }

    /* 11. WPSM */
    .wpsm { background: var(--white); margin-top: clamp(37px, 6.2svh, 62px); }
    .wpsm-grid { display: grid; grid-template-columns: 1.2fr 0.8fr; gap: clamp(36px, 5vw, 60px); align-items: center; }
    .wpsm-badge { background: var(--navy); border-radius: 2px; padding: clamp(32px, 5svh, 48px); text-align: center; color: var(--white); position: relative; overflow: hidden; }
    .wpsm-badge::before { content: ''; position: absolute; inset: 0; background: radial-gradient(circle at 30% 70%, rgba(217, 48, 37, 0.15) 0%, transparent 50%); }
    .wpsm-badge-number { font-family: var(--font-heading); font-size: clamp(48px, 7vw, 72px); font-weight: 800; line-height: 1; margin-bottom: clamp(4px, 1svh, 8px); position: relative; }
    .wpsm-badge-text { font-size: clamp(14px, 1.6vw, 16px); opacity: 0.8; position: relative; line-height: 1.5; }
    .wpsm-badge-label { margin-top: clamp(16px, 3svh, 24px); padding-top: clamp(16px, 3svh, 24px); border-top: 1px solid rgba(255, 255, 255, 0.15); font-family: var(--font-heading); font-weight: 600; font-size: clamp(12px, 1.4vw, 14px); letter-spacing: 1px; text-transform: uppercase; position: relative; }

    /* 12. WARUM LUFT-WASSER */
    .why { background: var(--off-white); }
    .why-hero { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(36px, 5vw, 60px); align-items: center; margin-bottom: clamp(32px, 5svh, 48px); }
    .why-hero-img { border-radius: 2px; overflow: hidden; height: clamp(280px, 36svh, 360px); }
    .why-hero-img img { width: 100%; height: 100%; object-fit: cover; }
    .why-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: clamp(16px, 2.5vw, 24px); margin-bottom: clamp(28px, 5svh, 40px); }
    .why-card { background: var(--white); border-radius: 2px; padding: clamp(20px, 3svh, 28px) clamp(18px, 2.5vw, 24px); border: 1px solid var(--border); }
    .why-icon { width: clamp(40px, 5vw, 48px); height: clamp(40px, 5vw, 48px); background: var(--blue-tint); border-radius: 2px; display: flex; align-items: center; justify-content: center; margin-bottom: clamp(12px, 2svh, 16px); }
    .why-icon svg { width: clamp(20px, 2.5vw, 24px); height: clamp(20px, 2.5vw, 24px); stroke: var(--navy); fill: none; stroke-width: 1.8; }
    .why-card h4 { margin-bottom: clamp(6px, 1svh, 8px); font-size: clamp(14px, 1.6vw, 16px); }
    .why-card p { font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); line-height: 1.6; }
    .why-cta-row { display: flex; align-items: center; justify-content: space-between; gap: clamp(20px, 3vw, 32px); margin-top: 8px; flex-wrap: wrap; }
    .why-calc-hint { background: var(--blue-tint); border-radius: 2px; padding: clamp(16px, 3svh, 24px) clamp(20px, 2.5vw, 28px); display: flex; align-items: center; gap: clamp(14px, 2vw, 20px); }
    .why-calc-hint p { font-size: clamp(13px, 1.5vw, 15px); font-weight: 600; color: var(--navy); white-space: nowrap; }

    /* 12a. KLIMAANLAGEN (neues Layout) */
    .klima { background: var(--white); }
    .klima-header { display: grid; grid-template-columns: 1.2fr 0.8fr; gap: clamp(32px, 4vw, 48px); align-items: center; margin-bottom: clamp(32px, 5svh, 48px); }
    .klima-header-text p { font-size: clamp(14px, 1.6vw, 16px); color: var(--mid-gray); line-height: 1.7; margin-top: clamp(12px, 2svh, 16px); }
    .klima-header-img { border-radius: 2px; overflow: hidden; height: clamp(240px, 32svh, 320px); background: linear-gradient(135deg, var(--navy) 0%, var(--navy-light) 100%); }
    .klima-header-img img { width: 100%; height: 100%; object-fit: cover; }
    .klima-types-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: clamp(16px, 2.5vw, 24px); margin-bottom: clamp(28px, 5svh, 40px); }
    .klima-type-card { background: var(--off-white); border-radius: 2px; padding: clamp(20px, 3svh, 28px) clamp(18px, 2.5vw, 24px); border: 1px solid var(--border); transition: transform 0.3s, box-shadow 0.3s; }
    .klima-type-card:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(0, 0, 0, 0.06); }
    .klima-type-icon { width: clamp(40px, 5vw, 48px); height: clamp(40px, 5vw, 48px); background: var(--blue-tint); border-radius: 2px; display: flex; align-items: center; justify-content: center; margin-bottom: clamp(12px, 2svh, 16px); }
    .klima-type-icon svg { width: clamp(20px, 2.5vw, 24px); height: clamp(20px, 2.5vw, 24px); stroke: var(--navy); fill: none; stroke-width: 1.8; }
    .klima-type-card h4 { margin-bottom: clamp(6px, 1svh, 8px); font-size: clamp(15px, 1.7vw, 17px); color: var(--navy); }
    .klima-type-card p { font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); line-height: 1.65; }
    .klima-cta { text-align: center; padding-top: 8px; }
    .klima-cta p { font-size: clamp(14px, 1.6vw, 16px); color: var(--mid-gray); margin-bottom: clamp(14px, 2.5svh, 20px); }

    /* 12b. KOSTENRECHNER */
    .calculator { background: var(--white); }
    .calc-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(32px, 4vw, 48px); align-items: start; }
    .calc-inputs { display: flex; flex-direction: column; gap: clamp(20px, 3.5svh, 28px); }
    .calc-field label { display: block; font-family: var(--font-heading); font-size: clamp(13px, 1.4vw, 14px); font-weight: 600; color: var(--dark); margin-bottom: clamp(8px, 1.2svh, 10px); }
    .calc-slider-row { display: flex; align-items: center; gap: clamp(12px, 1.5vw, 16px); }
    .calc-slider { flex: 1; -webkit-appearance: none; appearance: none; height: 6px; border-radius: 3px; background: var(--light-gray); outline: none; cursor: pointer; }
    .calc-slider::-webkit-slider-thumb { -webkit-appearance: none; appearance: none; width: 22px; height: 22px; border-radius: 50%; background: var(--navy); cursor: pointer; border: 3px solid var(--white); box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2); }
    .calc-slider::-moz-range-thumb { width: 22px; height: 22px; border-radius: 50%; background: var(--navy); cursor: pointer; border: 3px solid var(--white); box-shadow: 0 2px 6px rgba(0, 0, 0, 0.2); }
    .calc-value { font-family: var(--font-heading); font-weight: 600; font-size: clamp(13px, 1.5vw, 15px); color: var(--navy); min-width: 110px; text-align: right; white-space: nowrap; }
    .calc-toggle-group { display: flex; gap: 8px; }
    .calc-toggle { flex: 1; padding: clamp(8px, 1.2svh, 10px) clamp(12px, 1.5vw, 16px); border: 2px solid var(--border); border-radius: 3px; background: var(--white); font-family: var(--font-body); font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); cursor: pointer; transition: all 0.2s; text-align: center; }
    .calc-toggle:hover { border-color: var(--navy); color: var(--dark); }
    .calc-toggle.active { background: var(--navy); border-color: var(--navy); color: var(--white); }
    .calc-results { background: var(--off-white); border-radius: 2px; padding: clamp(24px, 4svh, 36px); border: 1px solid var(--border); text-align: center; }
    .calc-results .btn { display: flex; justify-content: center; margin: 0 auto; }
    .calc-result-card { text-align: center; margin-bottom: clamp(20px, 3.5svh, 28px); }
    .calc-result-header { font-family: var(--font-heading); font-size: clamp(12px, 1.4vw, 14px); font-weight: 600; color: var(--mid-gray); text-transform: uppercase; letter-spacing: 1px; margin-bottom: clamp(6px, 1svh, 8px); }
    .calc-saving { font-family: var(--font-heading); font-size: clamp(36px, 4.5vw, 48px); font-weight: 800; color: var(--navy); line-height: 1.1; }
    .calc-breakdown { display: flex; flex-direction: column; gap: clamp(8px, 1.5svh, 12px); margin-bottom: clamp(16px, 3svh, 24px); text-align: left; }
    .calc-row { display: flex; justify-content: space-between; align-items: center; }
    .calc-label { font-size: clamp(13px, 1.4vw, 14px); color: var(--mid-gray); }
    .calc-hint { font-size: clamp(11px, 1.2vw, 12px); opacity: 0.7; }
    .calc-data { font-family: var(--font-heading); font-weight: 600; font-size: clamp(13px, 1.5vw, 15px); color: var(--dark); }
    .calc-divider { height: 1px; background: var(--border); margin: 4px 0; }
    .calc-row-highlight .calc-data { color: var(--navy); font-size: clamp(15px, 1.7vw, 17px); }
    .calc-row-old .calc-data { color: var(--red); font-size: clamp(15px, 1.7vw, 17px); text-decoration: line-through; opacity: 0.7; }
    .calc-disclaimer { font-size: clamp(11px, 1.2vw, 12px); color: var(--mid-gray); line-height: 1.5; margin-top: clamp(14px, 2.5svh, 20px); text-align: left; }

    /* 13. REGION */
    .region { background: var(--navy); color: var(--white); position: relative; overflow: hidden; }
    .region::before { content: ''; position: absolute; inset: 0; background-image: linear-gradient(rgba(255,255,255,0.02) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px); background-size: 60px 60px; }
    .region-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(36px, 5vw, 60px); align-items: center; position: relative; }
    .region h2 { color: var(--white); }
    .region p { color: rgba(255, 255, 255, 0.75); margin-bottom: clamp(12px, 2svh, 16px); }
    .region-facts { display: flex; flex-direction: column; gap: clamp(14px, 2.5svh, 20px); margin: clamp(24px, 4svh, 32px) 0; }
    .region-fact { display: flex; align-items: center; gap: clamp(12px, 1.5vw, 16px); }
    .region-fact-icon { width: clamp(36px, 4.5vw, 44px); height: clamp(36px, 4.5vw, 44px); background: rgba(255,255,255,0.08); border-radius: 2px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
    .region-fact-icon svg { width: clamp(18px, 2.2vw, 22px); height: clamp(18px, 2.2vw, 22px); stroke: var(--white); fill: none; stroke-width: 1.8; }
    .region-fact-text { font-size: clamp(13px, 1.5vw, 15px); color: rgba(255,255,255,0.85); }
    .region-fact-text strong { color: var(--white); }
    .region-map { background: var(--navy-light); border-radius: 2px; height: clamp(280px, 36svh, 360px); overflow: hidden; border: 1px solid rgba(255,255,255,0.08); }
    .region-map iframe { width: 100%; height: 100%; border: 0; border-radius: 2px; filter: saturate(0.3) brightness(0.8); transition: filter 0.3s; }
    .region-map:hover iframe { filter: saturate(0.6) brightness(0.9); }

    /* 14. KONTAKT */
    .contact { background: var(--white); }
    .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(36px, 5vw, 60px); align-items: start; }
    .contact-info-item { display: flex; align-items: flex-start; gap: clamp(12px, 1.5vw, 16px); margin-bottom: clamp(16px, 3svh, 24px); }
    .contact-icon { width: clamp(36px, 4.5vw, 44px); height: clamp(36px, 4.5vw, 44px); background: var(--blue-tint); border-radius: 2px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
    .contact-icon svg { width: clamp(18px, 2vw, 20px); height: clamp(18px, 2vw, 20px); stroke: var(--navy); fill: none; stroke-width: 1.8; }
    .contact-info-item h4 { margin-bottom: 2px; font-size: clamp(13px, 1.5vw, 15px); }
    .contact-info-item p { font-size: clamp(13px, 1.5vw, 15px); color: var(--mid-gray); }
    .contact-info-item a { color: var(--navy); font-weight: 600; font-size: clamp(16px, 1.8vw, 18px); }
    .contact-hint { margin-top: clamp(24px, 4svh, 32px); padding: clamp(18px, 3svh, 24px); background: var(--blue-tint); border-radius: 2px; }
    .contact-hint p { font-size: clamp(13px, 1.5vw, 15px); color: var(--navy); }
    .contact-form { background: var(--off-white); border-radius: 2px; padding: clamp(28px, 4svh, 40px); border: 1px solid var(--border); }
    .form-group { margin-bottom: clamp(14px, 2.5svh, 20px); }
    .form-group label { display: block; font-size: clamp(13px, 1.4vw, 14px); font-weight: 600; margin-bottom: 6px; color: var(--dark); font-family: var(--font-heading); }
    .form-group input, .form-group textarea, .form-group select { width: 100%; padding: clamp(10px, 1.5svh, 12px) clamp(12px, 1.5vw, 16px); border: 1px solid var(--border); border-radius: 3px; font-family: var(--font-body); font-size: clamp(14px, 1.5vw, 15px); color: var(--dark); background: var(--white); transition: border-color 0.2s, box-shadow 0.2s; }
    .form-group input:focus, .form-group textarea:focus, .form-group select:focus { outline: none; border-color: var(--navy); box-shadow: 0 0 0 3px rgba(27, 42, 74, 0.1); }
    .form-group textarea { resize: vertical; min-height: clamp(90px, 14svh, 120px); }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(12px, 1.5vw, 16px); }
    .form-submit { display: flex; justify-content: center; margin: 4px auto 0; }
    .form-note { font-size: clamp(12px, 1.2vw, 13px); color: var(--mid-gray); margin-top: clamp(8px, 1.5svh, 12px); text-align: center; }

    /* 15. FOOTER */
    .footer { background: var(--navy-dark); color: rgba(255,255,255,0.6); padding: clamp(40px, 7svh, 60px) 0 clamp(20px, 3.5svh, 30px); }
    .footer-grid { display: grid; grid-template-columns: 1.5fr 1fr 1fr; gap: clamp(32px, 4vw, 48px); margin-bottom: clamp(32px, 5svh, 48px); }
    .footer-brand { font-size: clamp(13px, 1.4vw, 14px); line-height: 1.7; }
    .footer-logo { margin-bottom: clamp(12px, 2svh, 16px); }
    .footer-logo img { height: clamp(28px, 3.5vw, 36px); width: auto; filter: brightness(0) invert(1); opacity: 0.85; }
    .footer h4 { color: var(--white); font-size: clamp(12px, 1.4vw, 14px); font-weight: 600; letter-spacing: 1px; text-transform: uppercase; margin-bottom: clamp(12px, 2svh, 16px); }
    .footer-links { list-style: none; display: flex; flex-direction: column; gap: clamp(8px, 1.2svh, 10px); }
    .footer-links a { font-size: clamp(13px, 1.4vw, 14px); transition: color 0.2s; }
    .footer-links a:hover { color: var(--white); }
    .footer-bottom { padding-top: clamp(16px, 3svh, 24px); border-top: 1px solid rgba(255,255,255,0.08); display: flex; justify-content: space-between; align-items: center; font-size: clamp(11px, 1.3vw, 13px); }
    .footer-legal { display: flex; gap: clamp(16px, 2.5vw, 24px); }
    .footer-legal a:hover { color: var(--white); }

    /* 16. ANIMATIONEN */
    .fade-up { opacity: 0; transform: translateY(30px); transition: opacity 0.7s ease, transform 0.7s ease; }
    .fade-up.visible { opacity: 1; transform: translateY(0); }

    /* 17. RESPONSIVE */
    @media (max-width: 968px) {
      :root { --header-height: 76px; }
      /* Mobile: Überschriften zentrieren */
      h2 { text-align: center; }
      .about-usp-heading { text-align: center; }
      .section-subheading { text-align: center; }
      .about-intro, .wpsm-grid, .contact-grid, .region-grid, .why-hero, .calc-grid, .klima-header { grid-template-columns: 1fr; }
      .steps-grid, .why-grid, .klima-types-grid { grid-template-columns: 1fr 1fr; }
      .services-grid { grid-template-columns: 1fr; }
      .about-usp-grid { grid-template-columns: 1fr 1fr; }
      .about-intro-img { order: -1; }
      .why-cta-row { flex-direction: column; align-items: center; text-align: center; }
      .why-calc-hint { flex-direction: column; text-align: center; gap: 12px; width: 100%; }
      .why-calc-hint p { white-space: normal; }
      /* Mobile: Herstellerlogos 2x2 Grid */
      .logos-track { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(16px, 3svh, 24px) clamp(24px, 6vw, 40px); justify-items: center; flex-wrap: unset; }
      .logos-track img { height: clamp(36px, 7vw, 48px); width: clamp(110px, 28vw, 150px); }
      .logos-track img.logo-sm { width: clamp(69px, 17.6vw, 94px); }
      .logos-track img.logo-md { width: clamp(85px, 21.6vw, 115px); }
      /* Mobile: Buttons zentrieren (ausser Hero) */
      .steps-cta { text-align: center; }
      .klima-cta { text-align: center; margin-bottom: clamp(12px, 2svh, 20px); }
      .calc-results { margin: 0 auto; }
      .btn-cta, .btn-cta-lg { min-width: 0; width: clamp(240px, 60vw, 320px); max-width: 100%; }
      .service-link { align-self: center; }
      .why-cta-row .btn { align-self: center; }
      /* Mobile: Hover-Effekte bei Karten entfernen */
      .service-card:hover { transform: none; box-shadow: none; border-color: var(--border); }
      .service-card:hover .service-link { background: transparent; color: var(--navy); }
      .service-card:hover .service-card-img img { transform: none; }
      .service-card-img img { transition: none; }
      .step-card:hover { transform: none; box-shadow: none; }
      .about-usp-card:hover { transform: none; box-shadow: none; }
      .klima-type-card:hover { transform: none; box-shadow: none; }
      /* Mobile: kein Hover auf Buttons */
      .btn-primary:hover { transform: none; box-shadow: none; }
      .btn-dark:hover { transform: none; box-shadow: none; }
      .btn-outline-dark:hover { transform: none; box-shadow: none; }
      .footer-stoerung:hover { background: transparent; color: var(--red) !important; border-color: rgba(217, 48, 37, 0.3); }
      .footer-grid { grid-template-columns: 1fr 1fr; }
      .nav { display: none; }
      .nav.open { display: flex; flex-direction: column; align-items: center; justify-content: center; position: fixed; top: 0; left: 0; right: 0; bottom: 0; padding-top: calc(var(--header-height) + env(safe-area-inset-top, 0px) - clamp(48px, 7.5svh, 64px)); padding-left: 24px; padding-right: 24px; padding-bottom: calc(env(safe-area-inset-bottom, 16px) + 16px); gap: clamp(20px, 3.5svh, 32px); background: var(--navy); z-index: 999; overflow: hidden; text-align: center; }
      body.menu-open { overflow: hidden; }
      body.menu-open .header { background: var(--navy) !important; backdrop-filter: none !important; -webkit-backdrop-filter: none !important; border-bottom-color: transparent !important; }
      body.menu-open .menu-toggle span { background: var(--white); }
      .nav.open > a { font-size: clamp(18px, 2.5svh, 22px); color: rgba(255,255,255,0.9); }
      .nav.open > a::after { display: none; }
      .mobile-menu-link { display: block !important; }
      .nav.open .btn-primary { display: flex; justify-content: center; background: var(--red); border: none; color: var(--white) !important; font-family: var(--font-heading); font-weight: 600; font-size: clamp(15px, 2svh, 17px); padding: clamp(12px, 1.8svh, 16px) 24px; border-radius: 3px; white-space: nowrap; width: 100%; max-width: 240px; }
      .nav.open .btn-primary::after { display: none; }
      .nav.open .btn-stoerung { display: flex !important; justify-content: center; background: transparent; color: var(--white) !important; font-family: var(--font-heading); font-weight: 600; font-size: clamp(15px, 2svh, 17px); padding: clamp(12px, 1.8svh, 16px) 24px; border-radius: 3px; border: 2px solid var(--red); white-space: nowrap; position: absolute; bottom: clamp(20px, 4svh, 36px); left: 50%; transform: translateX(-50%); width: calc(100% - 48px); max-width: 240px; }
      .nav.open .btn-stoerung::after { display: none; }
      .menu-toggle { display: flex; }
      /* Mobile: kein Dropdown, flache Liste — Unterpunkte ausblenden, "Leistungen" ersetzt sie */
      .nav-dropdown { display: none; }
      .nav-dropdown-trigger { display: none !important; }
      .nav-dropdown-menu { display: none; }
      .nav-dropdown-menu a:first-child { border: none; border-radius: 0; }
      .nav-dropdown-menu a:last-child { border-radius: 0; }
      .nav-dropdown-menu a:hover { background: none; }
      .hero-content { }
      .why-hero-img { height: 280px; }

      /* === MOBILE: Image Fade Edges (subtil) === */
      .why-hero-img {
        -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 2%, black 98%, transparent 100%);
        mask-image: linear-gradient(to bottom, transparent 0%, black 2%, black 98%, transparent 100%);
      }
      .klima-header-img {
        -webkit-mask-image: linear-gradient(to bottom, transparent 0%, black 2%, black 98%, transparent 100%);
        mask-image: linear-gradient(to bottom, transparent 0%, black 2%, black 98%, transparent 100%);
      }

      /* === MOBILE: Heatpump & Klima Bilder als Vollbreit-Streifen === */
      .why-hero-img {
        border-radius: 0;
        margin-left: -24px;
        margin-right: -24px;
        width: calc(100% + 48px);
        height: clamp(200px, 32svh, 260px);
      }
      .klima-header-img {
        border-radius: 0;
        margin-left: -24px;
        margin-right: -24px;
        width: calc(100% + 48px);
        height: clamp(200px, 32svh, 260px);
      }

      /* === MOBILE: Parallax Image Setup === */
      .why-hero-img img,
      .klima-header-img img,
      .service-card-img img {
        height: 130% !important;
        will-change: transform;
        transform: translateY(-11.5%);
      }

      /* === MOBILE: Soft Section Transitions (Reihenfolge B) === */
      /* Hero→Services(off-white)→Logos(white)→Wpsm(white)→Steps(off-white)→About(white)→Why(off-white)→Klima(white)→Calc(white)→Region(navy)→Contact(white)→Footer */
      .services { background: var(--off-white); }
      .wpsm { background: linear-gradient(to bottom, var(--white) 0%, var(--white) 100%); }
      .steps { background: linear-gradient(to bottom, var(--white) 0%, var(--off-white) 4px, var(--off-white) 100%); }
      .about { background: linear-gradient(to bottom, var(--off-white) 0%, var(--white) 4px, var(--white) 100%); }
      .why { background: linear-gradient(to bottom, var(--white) 0%, var(--off-white) 4px, var(--off-white) 100%); }
      .klima { background: linear-gradient(to bottom, var(--off-white) 0%, var(--white) 4px, var(--white) 100%); }
      .region { background: linear-gradient(to bottom, var(--white) 0%, var(--navy) 4px, var(--navy) 100%); }
      .contact { background: linear-gradient(to bottom, var(--navy) 0%, var(--white) 4px, var(--white) 100%); }
      .footer { background: linear-gradient(to bottom, var(--white) 0%, var(--navy-dark) 4px, var(--navy-dark) 100%); }
    }
    @media (max-width: 640px) {
      :root { --header-height: 68px; }
      .section-padding { padding: clamp(44px, 8svh, 64px) 0; }
      .services { padding-bottom: clamp(37px, 6.2svh, 62px); }
      .steps-grid, .why-grid, .klima-types-grid, .about-usp-grid { grid-template-columns: 1fr; }
      .form-row { grid-template-columns: 1fr; }
      .footer-grid { grid-template-columns: 1fr; }
      .footer-bottom { flex-direction: column; gap: 12px; text-align: center; }
      .hero-inner { padding-top: calc(var(--header-height) + clamp(14px, 3svh, 32px)); padding-bottom: clamp(24px, 4svh, 40px); align-items: flex-start; }
      .hero-content { max-width: 100%; }
      .hero-box { background: none; backdrop-filter: none; -webkit-backdrop-filter: none; padding: 0; border-radius: 0; display: block; }
      .hero h1 { font-size: clamp(28px, 5svh, 38px); }
      .hero-subtitle { font-size: clamp(18px, 3svh, 24px); }
      .hero-text { font-size: clamp(14px, 2.2svh, 17px); line-height: 1.6; max-width: 100%; font-weight: 600; margin-bottom: clamp(48px, 10svh, 72px); }
      .hero-buttons { flex-direction: column; align-items: stretch; }
      .hero-buttons .btn { text-align: center; }
      .hero-img-desktop { display: none !important; }
      .hero-img-mobile { display: block !important; object-position: center 20%; }
      .hero-overlay { background: rgba(247,248,250,0.78); backdrop-filter: blur(1.5px); -webkit-backdrop-filter: blur(1.5px); }
      .header { background: rgba(255,255,255,0.35); backdrop-filter: none; -webkit-backdrop-filter: none; box-shadow: none; }
      .header.scrolled { background: rgba(247,248,250,0.88); backdrop-filter: blur(6px); -webkit-backdrop-filter: blur(6px); box-shadow: 0 1px 12px rgba(0,0,0,0.06); }
      .hero-badge { font-size: clamp(11px, 1.6svh, 13px); }
      .wpsm-badge { padding: clamp(24px, 4svh, 32px); }
      .wpsm-badge-number { font-size: clamp(44px, 7svh, 56px); }
      .klima-header-img { height: clamp(160px, 26svh, 200px); }
      .calc-saving { font-size: clamp(30px, 5svh, 36px); }
      .calc-toggle-group { flex-direction: column; }
    }
  </style>
</head>
<body>
  <slot />
</body>
</html>
